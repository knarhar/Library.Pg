# Library.Pg

A small book catalogue built with EF Core + PostgreSQL (running in Docker).


## Files
 
- `Book.cs` — the entity.
- `LibraryContext.cs` — the `DbContext`, connection string, and model config.
- `Program.cs` — everything else: seeding, queries, update/delete, bonus tasks.
